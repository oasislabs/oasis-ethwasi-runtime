const Storage = artifacts.require("Storage")

contract("Storage", (accounts) => {
  it("Retrieve storage from the fetch_bytes interface", async () => {
    //let instance = await Storage.new();
    //assert.equal(await instance.get(), 2, "0x0102030405");
  })
})
